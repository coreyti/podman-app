#!/usr/bin/env ruby
tap  "homebrew/cask-versions"

# NOTE:
# - podman CLI and API versions must match exactly.
# - homebrew only allows installation of latest (3.2.0 at this point).
# - latest fedora package for podman (in testing) is 3.2.0-rc3.
# - so, `podman` is currently installed from src for 3.2.0-rc3.
# brew "podman"
brew "websocat"

cask "multipass"
cask "vagrant"
cask "virtualbox"
